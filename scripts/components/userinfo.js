import Popup from './popup.js';

export class UserInfo {

    constructor (data) {
        this._name = document.querySelector(data.name);
        this._authorInfo = data.authorInfo;
    }

    getUserInfo() {

    }

    setUserInfo() {

    }
}